'use client';

// Theme toggle component - currently disabled as dark mode has been removed
export default function ThemeToggle() {
  // Dark mode has been removed from the application
  return null;
}
